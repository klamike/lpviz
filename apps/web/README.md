# @lpviz/web

Web application composition layer for lpviz.

This workspace owns the VanillaJS/TypeScript app entrypoint, imperative UI mounts, services, canvas wiring, shell/layout code, and top-level styling. Core math/solver/polytope logic should continue to live in internal packages under `packages/`.

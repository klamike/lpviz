# @lpviz/web

Web application composition layer for lpviz.

This workspace owns the React app entrypoint, UI providers, UI panels, shell/layout components, and top-level styling. Core logic should continue to live in internal packages under `packages/`.
